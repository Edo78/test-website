{% comment %}
  Usa questo layout in ogni post Markdown:
    ---
    layout: layouts/post.liquid
    ---
{% endcomment %}

<article class="post">
  {% if cover %}<img src="{{ cover }}" alt="">{% endif %}

  <h1>{{ title }}</h1>

  <p class="meta">
    Pubblicato: {{ publishedAt | date: "%d %b %Y" }}
    {% if updatedAt %} — Aggiornato: {{ updatedAt | date: "%d %b %Y" }}{% endif %}
  </p>

  {% if updated and updateReason %}
    <aside><strong>Aggiornamento:</strong> {{ updateReason }}</aside>
  {% endif %}

  {{ content }}
</article>

